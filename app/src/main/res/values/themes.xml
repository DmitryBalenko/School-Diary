<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- ТЕМА ЗАПУСКУ (Сплеш з логотипом) -->
    <style name="Theme.App.Starting" parent="@android:style/Theme.Material.NoActionBar">
        <!-- Тут ми ставимо картинку splash_background на фон -->
        <item name="android:windowBackground">@drawable/splash_background</item>
        <item name="android:statusBarColor">#050505</item>
    </style>

    <!-- ОСНОВНА ТЕМА (Чорний екран) -->
    <style name="Theme.SchoolDiary" parent="@android:style/Theme.Material.NoActionBar">
        <item name="android:windowBackground">#050505</item>
        <item name="android:statusBarColor">#050505</item>
    </style>
</resources>